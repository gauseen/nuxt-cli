// import { storage } from '~/utils/common.js'

// export default function ({isHMR, app, route, params, redirect}) {
// 	const defaultLang = storage.get('i18n')
// 	const defaultLocale = app.i18n.fallbackLocale
// 	// If middleware is called from hot module replacement, ignore it
// 	console.log('isHMR : ', isHMR, defaultLang)
// 	if (isHMR) return
// 	// app.i18n.locale = defaultLang
// }
